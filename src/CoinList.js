import React, { Component } from 'react'

export default class CoinList extends Component {
  

        const {currencyOptions} = this.props;
    
        const coinList = currencyOptions.map(name => {
          return(
            <li>{asdas}</li>
          )
        })

      